# Secrets — NEVER commit real API keys (use .env.example as the template)
.env

# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Agent runtime state (memory, FAISS index, artifacts, sandbox)
# The retrieval corpus (sandbox/papers/) IS committed; the rest of the
# sandbox is runtime scratch created by agent runs.
state/
My_Assignment/sandbox/*
!My_Assignment/sandbox/papers/
usage.json
/memory.json
/index_ids.json

# Personal notes and transcripts (kept local, not published)
My_Notes/
RAG_COURSE_NOTES.md
RAG_INTERVIEW_QUESTIONS.md
Session_7 INFO.txt
Session_7_Transcript.txt
